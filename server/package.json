{
  "name": "streambooru-server",
  "version": "0.2.0",
  "private": true,
  "type": "commonjs",
  "main": "src/index.js",
  "scripts": {
    "start": "bun src/index.js",
    "migrate": "bun src/migrate.js",
    "start:prod": "bun run migrate && (test -d webapp || cp -r ../renderer webapp 2>/dev/null || true) && bun run start"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.11.5"
  }
}
